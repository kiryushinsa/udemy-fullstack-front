export class Product {
    // сopy of json returned on previos steps data rest api
    id: string;
    sku: string;
    name: string;
    description: string;
    unitPrice: number;
    imageUrl: string;
    active: boolean;
    unitsInStock: number;
    dareCreated: Date;
    lastUpdated: Date;
}
